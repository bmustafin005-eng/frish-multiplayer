# ФРИШ Multiplayer Test v0.3

Тестовый сервер комнат ФРИШ.

В этой версии веб-интерфейс встроен прямо в `server.js`, поэтому отдельная папка `public` не требуется.

Render:
- Build Command: `npm install`
- Start Command: `npm start`
